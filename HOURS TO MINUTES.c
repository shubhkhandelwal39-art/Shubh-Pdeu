#include<stdio.h>
float main()
{
    float a,b;
    printf("Enter hours that is needed to convert into minutes \n");
    scanf("%f",&a);
    b=a*60;
    printf("The conversion of requied hours into minutes is %f",b);
}
