#include<stdio.h>
float main ()
{
    float a,b;
    printf("Enter minutes that is needed to be convert in hours \n");
    scanf("%f",&a);
    b=a/60;
    printf("hours as per conversion is %f",b);
}
