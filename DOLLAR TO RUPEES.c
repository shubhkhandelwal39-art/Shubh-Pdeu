#include<stdio.h>
float main()
{
    float a,b;
    printf("Enter the amount of dollar that needed to convert into rupees rate of conversion is 48 rupees \n");
    scanf("%f",&a);
    b=a*48;
    printf("converted amount in rupees is %f",b);
}
